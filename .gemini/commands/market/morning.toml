description = "Fetch the payload, archive it, render deterministic tables, optionally generate LLM summaries, and finalize the markdown locally."
prompt = """
!{python scripts/generate_morning_report.py {{args}}}
"""
